#include<iostream>
#include<string>
using std::string;
using std::cout; 
using std::cin; 
using std::endl;

int main(){
  // unsigned cnt = 42;
  // onstexprigned sz= 42;
  // cnt arr[10];
  // int *pazz[sz];
  // string bad[cnt]; // 错误
  // string strs[get_size()];  当get_size是constexpr的时候是正确;

  int a[] = {0, 1, 2};
  int a2[10];
  a2 = a; // 错误;
  for (auto i : scores)
    std::cout << i + " " << std::endl;

  return 0;
}
