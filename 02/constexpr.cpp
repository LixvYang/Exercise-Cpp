#include<iostream>

void main() {
  // 是否是常量表达式
  const int max_files = 20;
  const int limit = max_files + 1;
  int staff_size = 27;
  const int sz = get_size()

  // constexpr变量
  constexpr int mf = 20;
  constexpr int limit = mf+1;
  constexpr int sz = get_size();

  const int *p = nullptr;
  constexpr int *1 = nullptr;

}
