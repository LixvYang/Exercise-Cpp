#include<iostream>

void main() {
  typedef double wages;

  using SI = Sales_item;
  wages hourly, weekly;
  SI item;

  type char *pstring;
  const pstring cstr = 0;
  const pstring *ps;

  const char *cstr = 0;

  auto item = val1 + val2;
  auto i = 0, *p = &i;
  auto sz = 0, pi = 3.14 // 错误

  decltype(f()) sum = x;


}